import { Grid, Text, Image, Button, Wrapper } from '../elements';



const Signup = (props) => {
    return(
        <Grid>
            <Text>회원가입</Text>
        </Grid>

    );
}

export default Signup;